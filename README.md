This is a minimal project setup for using TypeScript + Express.

## Usage

Run `yarn` to install dependencies then run `yarn start` to start the server.

## Tests

Run `yarn test` to start the test runner.
